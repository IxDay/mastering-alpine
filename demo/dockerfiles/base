FROM alpine:3.5

ARG PUBKEY
ARG REPO_PORT

RUN echo "http://$(hostname -i|cut -d'.' -f1-3).1:$REPO_PORT" > /etc/apk/repositories && \
	echo "http://dl-cdn.alpinelinux.org/alpine/v3.5/community" >> /etc/apk/repositories

COPY assets/keys/${PUBKEY} /etc/apk/keys
RUN apk update
